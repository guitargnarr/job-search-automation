# Sentinel-1 System Dependencies
# Python 3.9+ required

# Core database and data handling
# sqlite3 is built-in, no installation needed

# Document generation
python-docx>=1.1.0
reportlab>=4.0.0

# Web and API integration (legacy, kept for compatibility)
requests>=2.31.0
google-api-python-client>=2.108.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Data processing
pandas>=2.1.0

# System utilities
# pathlib and datetime are built-in Python modules
